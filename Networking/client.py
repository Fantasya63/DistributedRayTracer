import socket
import threading

# SERVER_IP = "192.168.8.38"
# PORT = 6376
# connection_address = (SERVER_IP, PORT)


# client = socket.socket(socket.AF_INET, socket.SOCK_STREAM)

# MESSAGE_HEADER_LENGTH = 1


# def parse_message(header : int):
#     print(f"Header recieved: {header}")


# def main():
#     print("Connecting to server...")
#     client.connect(connection_address)

#     terminate = False
#     while not terminate:
#         header = int(client.recv(MESSAGE_HEADER_LENGTH))
#         parse_message(header)
